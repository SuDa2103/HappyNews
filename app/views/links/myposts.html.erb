<div class="container">
  <br>
  <p>These are your saved posts, for those times when you need to revisit!</p>
<section class="all-links">
  <% @links.each do |link|  %>
    <div class="link">
     <div class="link-top"> 
      <%= link_to upvote_link_path(link), method: :post  do %>
        <%= image_tag "logo.png", class: "upvote-img"%>
      <% end %>
      <div class="title">
        <%= link_to link.title, (link.url? ? link.url : link) %>
        <% if link.url? %>
          <span>
            ( <%= link.url %> )
          </span>
        <% end %>
      </div>
     </div>
      <div class="metadata">
        <span class="points">
          <%= pluralize(link.points, 'point') %> by <%= link.user.username %>
        </span>
        <span class="time-created">
          <%= time_ago_in_words(link.created_at) %> ago
          <span class="comment-count">
              <%= link_to pluralize(link.comment_count, 'comment'), link %>
          </span>

          <% if user_signed_in? && current_user.owns_link?(link) %>
            <span class="edit-link">
              <%= link_to "edit", edit_link_path(link) %>
            </span>
            <span class="delete-link">
              <%= link_to "delete", link, method: :delete, data: { confirm: "Are you sure?" } %>
            </span>
          <% end %>

          
            <span class="upvote-link">
               <%= link_to "(#{link.upvotes})", upvote_link_path(link), method: :post %>
            </span>
          

        </span>
      </div>

    </div>
  <% end %>
</section>

